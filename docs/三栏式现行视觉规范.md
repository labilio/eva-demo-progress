# 三栏式现行视觉规范

2026-09-07 用户明确确认：同事的三栏式样式调整正确，规范随之更新。基准来源 main a6213ed；之后已修复的公共头像比例、AI 标跟名、身份范围与表单兼容问题继续保留。

## 尺寸与表面

| 元素 | 当前合同 |
| --- | --- |
| 一级导航 | 默认 180px，折叠 80px；仅承载一级入口 |
| 会话中栏 | 默认 260px，拖拽范围 220–480px；使用共享 current 变量 |
| 中栏底色 / 边框 | #f6f7f8 / #dfe3e8 |
| 我的 AI 身份行 / session 行 | 34px / 38px 最小高度 |
| 我的 AI 层级缩进 | 行内边距 8px，头像槽 22px，文字间隔 8px；子级文字起点 58px |
| 我的 AI 顶部操作 | 标题与 32px 新建按钮同排 |
| 个人 Eva session 行 | 32px 最小高度，沿用个人组件 |

## 文字与状态

- 团队消息及我的 AI 使用现行 GDS label 14px、caption 12px；名称中等字重，会话标题常规字重。具体行高取对应语义 token，不重新手写一套。
- 个人 Eva 的历史时间保留现有 11px/20px；业务差异不通过强制全部同字号消除。
- 我的 AI 当前主文字 #1f2329、次文字 #30343b、时间 #8d95a1；hover #e2e3e5、选中 #e5e6eb。
- 个人列表 hover/选中使用 --eva-overlay-hover / --eva-overlay-pressed（当前为黑色 6% / 9% 叠层）。这是现有入口差异，不把两个值混成新颜色。
- 长标题应截断并保留时间与操作空间；AI 标随姓名同组排列，不能被姓名 flex 撑到行尾。

## 适用边界

用户认可的是三栏外观与布局；不改变 AI 身份归属、AI 标显示场景、公共头像比例、项目访问权限、任务编号、会话数据及输入行为。个人 Eva 不强制改成团队 IM。

消息内嵌项目、我的 AI 协作图标及批注悬浮位置按用户确认保留。我的 AI 位于个人模块并紧跟 Eva 同学；团队协作不再重复显示该入口。

## 实现与验收

尺寸来自 009-7-patch-sider.js、043-final-layout-convergence.css；团队身份列表来自 046-ai-team.css；文字 token 来自 008-gds-tokens.css；个人列表来自 043 与个人页模块。修改前须同时读实际组件，不能仅依据本文数值覆盖代码。

同视口、同缩放比较个人 Eva、消息和我的 AI，检查长名称、折叠、拖拽、hover、选中、头像与 AI 标。源码数值核对不是浏览器视觉验收。


## 2026-09-08 中栏对齐修正

固定群、分类标题及 AI 身份名称采用同一文字起点；固定群和 AI 身份行统一 34px，子区及会话行 38px。各身份展开箭头固定在同一右侧列，不再受新建会话／更多操作按钮数量影响。操作按钮在悬停或键盘聚焦时显示，当前身份仍保留新建入口。分组之间统一使用 16px 留白，移除分割线。

本次仅修改 AI 团队中栏样式，消息、草稿和成员数据逻辑不变。前后截图见仓库同级 `review-0908/index.html` 第七项。


## 2026-09-08 用户再次明确的我的 AI 层级

中栏顶部显示「我的 AI」及 32px 新建按钮，下方先分「AI 团队」「AI 助理」两区。两个顶层分区使用带专属图标、较强字重和独立留白的 40px 无框标题条；不得增加整行卡片背景、描边或阴影。云端分身、个人助理、数字员工仍使用更紧凑的轻量角色标题，不能再让两个层级共用相同的“箭头＋文字＋数量”外观。两区仍可独立展开／收起，但本轮不新增收起状态专属样式。默认团队名称为「我的 OPT」；默认团队和自定义团队保留圆形群头像、名称及可展开子区，团队行不显示成员数，默认团队额外显示“默认”标记。AI 助理继续按云端分身、个人助理、数字员工分组，身份及会话维持原有缩进。此条覆盖上一节仅展示单一固定群的历史规则。

## 2026-09-08 个人首页恢复决定

个人新会话首页以 09-07 v10（9c904d4）为基准：居中「AI随行／Eva头像／工作随心」、输入框、下方快捷能力排；不使用右侧人物与 hi 气泡，也不将快捷能力放回输入框上方。保留原生 textarea 的中文输入、粘贴及技能选择修复，后续 GDS 调整不得覆盖这一已确认布局。

## 2026-09-09 我的 AI 一级分区色带

用户最新决定覆盖 2026-09-08 关于顶层分区不得使用整行背景的限制。「AI 团队」「AI 助理」两个一级分区标题统一使用低饱和雾紫底色（默认 #f4f1fa、hover #eee9f7、按下 #e8e1f3）和 6px 小圆角，底色覆盖标题整行；不增加描边或阴影。标题条按「专属图标＋标题」左对齐、「数量＋折叠箭头」右对齐排列，数量与箭头使用 #817a8c，整条标题仍是同一个折叠按钮。云端分身、个人助理、数字员工等二级角色标题继续使用透明背景，团队、身份、子区及会话行的 hover／选中态保持原合同，以此明确一级分区与下级内容的层级关系。

## 2026-09-09 我的 AI 子级缩进

用户最新决定覆盖 2026-09-08 关于固定群、分类标题及 AI 身份名称同起点的规则。团队子区图标与 AI 助理身份头像统一作为二级内容，其左边缘比「我的 OPT」等一级团队头像向右缩进 12px；身份下的会话标题在身份名称基础上继续向右缩进 12px。一级标题、团队行和二级角色分组标题位置不变，折叠箭头仍固定在右侧操作列。

## 2026-09-09 取消我的 AI 一级分区色带

用户最新决定覆盖本日「我的 AI 一级分区色带」规则。「AI 团队」「AI 助理」默认使用透明底色，不保留雾紫色带、描边或阴影；仅在 hover／按下时使用现有中性交互层。数量与折叠箭头恢复中性色。一级层级继续通过专属图标、较强字重、40px 标题高度、分区留白和区间分割线表达，子级缩进规则保持不变。

## 2026-09-09 我的 AI 助理缩进收敛

用户最新决定覆盖本日「我的 AI 子级缩进」中关于 AI 助理身份的规则。云端分身、个人助理、数字员工下的身份头像左边缘与对应分类标题文字对齐；身份下的新对话及历史会话标题与身份名称列对齐，不再额外向右缩进 12px。AI 团队子区仍保持其图标比团队头像向右 12px，两套缩进使用独立变量，避免后续调整互相影响。

## 2026-09-09 会话与团队子区文字对齐

用户最新决定细化本日「我的 AI 助理缩进收敛」规则。AI 助理身份下的新会话及历史会话，其文字起点与上方 AI 团队子区名称的文字起点严格对齐；不与子区图标左边缘对齐。子区的 14px 图标槽和 6px 图文间距纳入共享缩进变量，身份头像与角色分类标题的对齐关系保持不变。

## 2026-09-09 会话改为与助理名称对齐

用户最新决定覆盖本日「会话与团队子区文字对齐」规则。AI 助理身份下的新会话及历史会话，文字起点改为与「王宜林的 AI 分身」「通用助理」等助理名称严格对齐；不再跟随上方 AI 团队子区名称列。身份头像与角色分类标题的对齐、团队子区缩进及其他层级位置保持不变。

## 2026-09-09 我的 AI 首次进入折叠规则

页面每次首次挂载时，云端分身、个人助理、数字员工下的身份会话默认全部收起；AI 团队中的自定义团队子区默认收起。默认团队「我的 OPT」子区默认展开，并按 `updated_at`／`updatedAt` 从新到旧仅展示最新 3 条；存在更多子区时在同一列表末尾显示“展开查看（N）”，展开后可通过“收起”恢复三条预览。带 `evaIdentity`／`evaSession` 的直接访问仍应自动展开目标，用户在本次页面停留期间的手动展开状态不得被重置。
