# 三栏式现行视觉规范

2026-09-07 用户明确确认：同事的三栏式样式调整正确，规范随之更新。基准来源 main a6213ed；之后已修复的公共头像比例、AI 标跟名、身份范围与表单兼容问题继续保留。

## 尺寸与表面

| 元素 | 当前合同 |
| --- | --- |
| 一级导航 | 默认 180px，折叠 80px；仅承载一级入口 |
| 会话中栏 | 默认 260px，拖拽范围 220–480px；使用共享 current 变量 |
| 中栏底色 / 边框 | #f6f7f8 / #dfe3e8 |
| AI 团队身份行 / session 行 | 34px / 38px 最小高度 |
| AI 团队层级缩进 | 行内边距 8px，头像槽 22px，文字间隔 8px；子级文字起点 58px |
| AI 团队顶部按钮 | 34px 高 |
| 个人 Eva session 行 | 32px 最小高度，沿用个人组件 |

## 文字与状态

- 团队消息及 AI 团队使用现行 GDS label 14px、caption 12px；名称中等字重，会话标题常规字重。具体行高取对应语义 token，不重新手写一套。
- 个人 Eva 的历史时间保留现有 11px/20px；业务差异不通过强制全部同字号消除。
- AI 团队当前主文字 #1f2329、次文字 #30343b、时间 #8d95a1；hover #e2e3e5、选中 #e5e6eb。
- 个人列表 hover/选中使用 --eva-overlay-hover / --eva-overlay-pressed（当前为黑色 6% / 9% 叠层）。这是现有入口差异，不把两个值混成新颜色。
- 长标题应截断并保留时间与操作空间；AI 标随姓名同组排列，不能被姓名 flex 撑到行尾。

## 适用边界

用户认可的是三栏外观与布局；不改变 AI 身份归属、AI 标显示场景、公共头像比例、项目访问权限、任务编号、会话数据及输入行为。个人 Eva 不强制改成团队 IM。

消息内嵌项目、同事的我的 AI 协作图标及批注悬浮位置按用户确认保留。导航名称仍为任务看板、团队协作、数字员工、我的 AI 团队。

## 实现与验收

尺寸来自 009-7-patch-sider.js、043-final-layout-convergence.css；团队身份列表来自 046-ai-team.css；文字 token 来自 008-gds-tokens.css；个人列表来自 043 与个人页模块。修改前须同时读实际组件，不能仅依据本文数值覆盖代码。

同视口、同缩放比较个人 Eva、消息和我的 AI 团队，检查长名称、折叠、拖拽、hover、选中、头像与 AI 标。源码数值核对不是浏览器视觉验收。


## 2026-09-08 中栏对齐修正

固定群、分类标题及 AI 身份名称采用同一文字起点；固定群和 AI 身份行统一 34px，子区及会话行 38px。各身份展开箭头固定在同一右侧列，不再受新建会话／更多操作按钮数量影响。操作按钮在悬停或键盘聚焦时显示，当前身份仍保留新建入口。分组之间统一使用 16px 留白，移除分割线。

本次仅修改 AI 团队中栏样式，消息、草稿和成员数据逻辑不变。前后截图见仓库同级 `review-0908/index.html` 第七项。


## 2026-09-08 用户再次明确的固定群层级

中栏「我的 AI 团队」去掉群头像，复用其他分类的下拉展开行。其下 thread 图标与 AI 头像同列，thread 名称与 AI 名称同列，均为 34px 行高；单一 AI 的会话层级保持原有缩进。点击标题展开／收起，并可返回固定群。此条覆盖上一节固定群子区缩进规则。
