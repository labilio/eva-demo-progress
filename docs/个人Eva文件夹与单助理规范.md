# 个人 Eva：单助理与文件夹

> 2026-09-10 最新决定：一级侧栏隐藏「Agent 创建中心」模块；创建中心页面与直达路由继续保留，但「我的 Agent」新建个人助理直接使用共享完整编辑弹窗，不再进入创建中心。数字员工市场和「我的 Agent」的新建菜单不受影响。

2026-09-08 用户确认个人「Eva 同学」主入口只有一个固定 Eva，取消该入口内的通用助理／研发助理分组、创建／编辑助理和右侧助理选择。中栏参考用户提供的 Codex 截图，用文件夹组织对话。用户进一步确认未归类也属于分类，统一命名“默认”。本决定只约束个人 Eva 主入口；个人模块下独立的「我的 AI」仍可创建和编辑多个个人助理。

- 顶部固定“新对话”，新对话默认归入“默认”；文件夹行内的加号在该文件夹内发起对话。首次发送时才保存对话。
- “默认”是固定分类，与其他文件夹使用相同折叠和会话列表组件，不能重复创建。文件夹只能创建，不提供助理配置或文件夹编辑。
- 文件夹只组织个人对话，不等同于 AI 身份、团队协作项目或电脑目录；示例文件夹沿用“供应链运营协同”名称，无额外项目实体或授权关系。
- 对话可重命名、移入其他文件夹或移回默认；保持稳定 ID 与历史内容。每组超过六条时提供展开／收起。
- 所有个人对话展示 Eva 同学；历史的五条对话均保留原 ID。团队 IM 仍使用独立数据仓与会话，团队侧也按用户追加确认收敛为同一个 Eva；旧助理的团队会话保持稳定 ID、内容、置顶和草稿。其他助理未发送的新会话草稿转为独立待发送会话，避免覆盖 Eva 的草稿。云端分身和数字员工保留各自身份；原来同步自已撤销助理的分身保留配置并转为独立来源。
- 个人文件夹、对话、折叠状态由 `EvaPersonal` 数据仓唯一管理，浏览器 localStorage 保存。它不提供云端同步。界面通过稳定 ID 读取详情；路由决定已选对话。中栏动作仅更新中栏，不重建右侧输入和消息。
- 新建上下文与各历史会话的草稿按 ID 隔离，离开组件时保存内存草稿；刷新恢复已发送消息与分类，不宣称草稿跨刷新持久化。
- Agent 创建中心移除“新建个人助理”及其创建流程；旧 `evaCreate=mine` 链接回到创建中心目录。云端分身与项目专业能力入口保留。
- 个人首页的居中欢迎、输入、技能及任务生命周期继续使用现行 GDS；取消右侧助理选择，显示当前文件夹，可通过下拉选择默认或已有文件夹，切换时保留正在输入的内容。团队消息和「我的 AI」布局不受此规范限制；「我的 AI」按 AI 团队与 AI 助理组织多个可用身份。
- 验收覆盖默认折叠、创建与重名校验、组内发起、移动和重命名、刷新恢复、同名对话按 ID 路由、中栏操作保留输入、入口往返及全局标题栏。

## 本轮本地验证

- 基线：`origin/main` 198930a；功能分支 `codex/personal-eva-folders`。
- `npm test`：296 项通过；构建、manifest、project、collaboration、消息路由、侧栏选中态、补丁 hash、生成 ES 模块语法与 diff 检查通过。
- Edge：`http://127.0.0.1:4185/`，三栏对比使用同一窗口和缩放（1212 × 858）；后续输入选择与迁移检查在 1212 × 906 窗口完成。
- 实际创建“本周工作”，重命名并移动原接口回归会话，刷新后保留；组内发送新请求后可重新打开历史。输入框下拉选择“供应链运营协同”时中文输入保持不变。
- 实际执行个人 Eva → 消息 → 我的 AI → 个人 Eva 的入口往返；个人 Eva 始终保持单一固定入口，「我的 AI」中的多个助理能打开各自会话，默认团队仍保留三条子区。系统标题栏保持可见。
- 创建中心保留云端分身与项目／专业能力，已无个人助理创建卡片。
- 本地测试内容仅保存在独立本地端口的浏览器存储，未写入共享批注或业务服务。生产版本号未修改，未合并 main。

## 2026-09-08 图标与选中态调整

- 文件夹行右侧为更多设置、新建对话；点击名称展开或收起。设置提供展开或收起、重命名、删除文件夹，固定“默认”不可重命名或删除。删除文件夹时保留对话并移回默认。
- 对话行悬停操作为置顶／取消置顶、删除，不提供归档；置顶在所属文件夹内优先显示，删除经确认后持久保存，刷新不恢复已删除的演示对话。右键保留重命名、移动能力。
- 悬停、按下与会话选中均使用浅灰色；选中会话不联动高亮文件夹。
- 本轮用户明确要求不执行任何测试，仅构建并打开本地预览；本轮未进行自动化或浏览器验收。

- 最新修正：文件夹最右侧为加号，点击在右侧打开该文件夹的新对话首页；更多操作为不占列表高度的迷你下拉菜单，顺序是重命名、删除、置顶／取消置顶。默认文件夹的重命名和删除禁用，允许置顶。文件夹置顶状态持久保存。

## 2026-09-08 后续确认

移除标题旁创建文件夹按钮；输入框文件夹选择器最后一项为无图标“浏览本地目录...”，选择本地目录后仅以目录名建立分类，不读取或上传文件内容。每次打开页面默认展开全部文件夹，仍可手动收起。置顶按钮只在悬停或键盘焦点进入时显示，置顶后不显示常驻图标。用户确认本轮合入 main；不运行本地测试，GitHub 发布检查仍由 CI 执行。

## 2026-09-09 分组与本地工作目录独立（覆盖上述绑定规则）

- 左侧统一称“分组”，用展开箭头表示平铺的会话分类，不使用文件夹图标；标题右侧常驻加号用于创建分组。顶部“新对话”采用轻描边按钮，默认归入默认分组。
- 保留已有分组和会话 ID、内容、折叠、置顶、重命名、删除与移动行为。底层沿用 folderId 和现有存储键以兼容历史数据，这些字段只表示视觉分类。
- 输入框下方的文件夹图标与“默认”表示本地工作目录。选择目录不创建分组、不移动会话；创建、切换、重命名、删除分组也不改变选定目录。
- 目录选择独立保存于个人页面内存，首次发送时仅将目录名称记入会话 workingDirectory 元数据。原型不读取或上传目录内容，不代表已接通本地执行器；刷新不恢复目录访问权限。旧分组名称不推断为工作目录。
- 创建分组仅需名称，空名、超长和重名校验复用数据仓。创建或取消时保留输入草稿；会话仍在首次发送后保存。

### 同日命名调整

左侧固定分组“默认”改称“最近”，移动菜单及删除回退文案同步；会话归属和排序行为不变。输入框未指定工作目录时显示“本地目录”，选择后仍显示实际目录名。

## 会话拖拽归组

可将会话标题拖到“最近”或任意自定义分组。有效目标高亮，释放后保存到既有会话数据仓，折叠目标自动展开。保留当前路由、正文、草稿和工作目录；原组内拖放不排序，取消及外部拖入不修改数据。右键移动入口继续保留。
