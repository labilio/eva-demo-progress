# 合并记录

## 来源

- 我的 AI 团队任务：`01a075c6-50bf-7203-b03d-6792099794b9` 与项目协作任务 `01a0756e-167a-7a42-9c58-7c33e4166418` 的最终本地工作稿。
- AI 最终快照：`ed7b5ae`（基于 `f1b17b6`）。
- 项目成员与文件最终快照：`7b02e28`（基于 `e3a1d12`）。
- 集成起点：`origin/main` 的 `e3a1d12`。两个原始工作区及其未提交内容均保留，集成在独立工作区与分支完成。

## 公共实现的合并方式

- 保留唯一构建补丁链和统一 IM 内核，同时接入 AI 身份会话数据与项目成员权限数据。
- 公共聊天信息组件按会话类型接收配置；AI 会话的草稿与置顶归 AI 会话状态管理，项目群成员与权限归成员数据管理。
- 头像与 AI 标记继续复用公共身份函数；两边的身份、路由、运行时和成员测试同时保留。
- 页面入口与 manifest 同步登记 69 个模块，删除已被项目成员实现替代的旧演示模块。
- 项目设置使用当前项目的名称与共同目标；统一添加成员选择器按同事邀请和本人分身加入执行各自流程。
- 项目演示身份切换仅作用于项目和群聊演示，个人 Eva 与我的 AI 团队继续使用王宜林的演示数据。

## 验证

- 78 项自动测试通过，覆盖 AI 会话与同步、成员权限、文件共享、项目信息持久化和公共运行时。
- 构建、manifest、项目合同、协作合同、消息路由、侧栏选中态、补丁哈希、生成 ES 模块语法、GDS 校验与差异格式检查通过。
- Edge 本地预览完成 AI／消息／项目入口往返、AI 草稿保留、聊天信息、普通私聊菜单、项目邀请审批、群文件转存及项目成员混合选择验证。
- 同事与分身混合添加：分身立即进入成员表，同事保持待本人接受；AI 头像保留来源和主人归属。
- 项目信息已检查名称和目标保存、刷新保留及普通成员只读。集成未对个人 Eva 和团队消息的基准页面增加样式改写。

正式部署的目标 commit、Preview 地址及质量检查以本次 GitHub PR 记录为准。
