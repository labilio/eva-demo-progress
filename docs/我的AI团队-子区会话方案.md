# 我的 AI 团队：子区承载会话

2026-09-07，用户明确批准实施。本文件取代 Step 1 中团队侧独立 OpenClaw session 的方案，个人 Eva 不在调整范围。

## 产品合同

- 中栏保留云端分身、数字员工分组、默认折叠与现行三栏尺寸。每个 AI 使用其全局一致的单张圆形头像，跟随姓名的 AI 标保持不变；不显示主人／归属小头像。
- 每个 AI 身份对应“当前本人＋该 AI”的专用两人群；每段对话对应这个群下的一个子区。该群作为会话容器，不提供父群公共聊天入口，不进入普通项目群列表。
- 对用户统一称“会话／对话”。不显示子区图标、群人数、父群／子区面包屑、成员邀请、退出群、群治理或子区归档等操作。
- 右侧标题只显示 AI 头像、姓名及 AI 标；会话名及重命名只在中栏提供。聊天信息直接复用普通私聊结构，只显示对象身份、免打扰、置顶及清空记录。
- 新建立即分配默认名称“新对话”，重名递增编号，可直接讲话。首次发送后取首条消息前 20 个字符作为名称；如果用户此前手动改名则保持自定义名称。改名接受 1–50 个字符，拒绝纯空白。
- 会话操作菜单提供重命名、置顶／取消置顶和删除。保留删除无需确认、删除当前项后选择同身份剩余会话、没有剩余时进入新对话的行为。置顶只在当前身份内排序。
- 本人直接发送就触发当前 AI；这是固定且唯一的方式，不能设置成必须 @。此规则仅适用于这种两人会话，普通项目群和子区仍按已有 @ 规则执行。
- 切换对话保持消息和草稿隔离；打开聊天信息或改名不重建消息流及输入区。仍只有一个客户端系统标题栏。

## 原型实现与状态归属

`009-3-ai-team-store.js` 提供共用的 `EvaAIPrivateConversations` 适配器：以稳定本人 ID 与 AI ID 推导唯一两人群，声明两名成员及 `direct-only` 回复策略；不复制人员或项目。`009-3-digital-employees-store.js` 使用同一个群／子区适配器。

子区使用 Octo 字段 `group_no`、`short_id`、`channel_id`、`channel_type: 5`，完整频道 ID 为 `group_no + "____" + short_id`。现有持久化集合名 `sessions`、旧记录 ID 及兼容 API 保留，含义已改为子区记录，不代表 OpenClaw session，也不同时维护第二份消息集合。读取及新建补齐群／子区关系，不替换已有标题、消息、草稿或置顶数据。

页面 React 组件唯一拥有当前身份／会话选择与弹窗状态。选择通过 `source.selectedThreadId` 传给既有 ChannelsView，走原有 `fa` 子区选择、`threadMessages` 消息读取与共享输入框。`presentation: ai-direct` 仅控制标题、头像、输入提示与聊天信息的私聊外观；底层 `chatType` 保持 group。

会话标题／置顶仍由各自业务 store 唯一维护。聊天信息复用 `EvaChatSettings`，通过配置回调执行改名和置顶，不再保存第二份置顶状态；免打扰和清空记录复用公共聊天偏好，按完整子区频道 ID 隔离。

本仓库仍是本地存储驱动的原型，AI 回复仍为既有模拟回执。本次未创建真实 Octo 服务端群、未连接真实 AI，不能把前端子区频道隔离表述为已验收的模型上下文隔离。后续服务联调需要按同一群／子区 ID 映射落地，并保证两人成员范围与固定免 @ 策略。

## 参考代码

只读参考本机固定版本 Octo-Web `c2e2aeed2d7027e3aa25f8c726798eb7cdba2463`：

- `packages/dmworkbase/src/Service/Thread.ts`：Thread 数据合同、type 5、频道 ID 分隔符及构建规则。
- `packages/dmworkdatasource/src/datasource.ts`：`threadCreate(groupNo, name)`、`threadUpdate(groupNo, shortId, {name})`、`threadDelete`。
- 项目当前 ChannelsView 与 EvaChatSettings：共用子区消息流、输入、消息操作和聊天信息，不另建 IM 内核。

## 本轮交付状态

用户明确要求“不测试”。本轮仅做源码修改及必要构建，不运行自动化测试、合同验收脚本或浏览器验收，不声称视觉、交互、迁移或真实 AI 调用已经验收。未修改正式版本号，未合并或推送 main，未发布 Production。


## 聊天信息复用私聊（用户纠正后的现行方案）

AI 两人群的右侧信息面板与普通私聊共用同一段渲染、身份卡、标题栏、间距、背景和设置分组，不提供 AI 专用面板变体。身份通过公共组件传入 32px 单张圆形头像、姓名及 AI 标；点击身份卡仍打开对象信息。右侧不出现会话名、子区名或群信息；重命名保留在中栏菜单。免打扰、置顶与清空记录继续作用于当前对话。


## 统一输入提示

所有团队 IM 输入框（人类私聊、AI、群聊、子区及子区侧栏）统一使用“发送给 ”＋当前对象名称，保留一个半角空格，不追加省略号。我的 AI 团队使用 AI 身份名；普通群聊及子区使用各自名称。离线仍由原有禁用状态控制，不改变这条文案规则。参考 Octo 的 `messageInput.placeholder.directWithName`。

该规则已提升为根目录 AGENTS.md 的项目级 IM 输入提示合同；共用 evaIMPlaceholder，并照搬 Octo 的无名称回退“发送消息”。

## 2026-09-08 用户调整

- 入口顺序为固定群「我的AI团队」、云端分身、个人助理、数字员工；移除顶部“添加 AI”。
- 个人助理与个人 Eva 共用名称及配置数据源，自动拥有独立 IM 两人群／子区；本地 chat session 的历史和草稿不导入 IM。
- 固定群成员由本人、全部云端分身、个人助理和已加入 AI 团队的数字员工动态推导。固定群不可手工改名、增删成员、转让或解散；成员信息、消息流、输入、提及和聊天偏好复用公共 IM 组件。
- 固定群是多人群，普通消息不会触发所有 AI，仅明确 @ 的 AI 产生原型回执；单一 AI 的两人群仍保持直接发送触发。这里只是本地原型数据，没有真实 channel adapter 网络调用。


## 2026-09-08 固定群保留子区

- 固定群「我的AI团队」复用消息里的群／子区层级，中栏可展开子区；右侧复用子区列表、创建表单、子区侧栏和消息输入。
- 群聊、每个子区各自保存消息和草稿。子区频道采用 `群 ID____子区短 ID`、`channel_type: 5`；不会把父群、其他子区或个人 Eva 的聊天记录并入当前子区。
- 子区继承固定群的动态成员及 @ 名单，沿用群聊的明确提及触发规则；列表摘要和回复数来自本子区消息。归档、改名、删除沿用已有子区操作。
- 此变更独立提交，接续前五项修改。构建、199 项自动化测试及 manifest／project／collaboration／路由／侧栏合同检查通过。Chrome 实际验证创建两个子区、父群与子区来回切换、刷新后的消息和草稿、侧栏发送及成员提及。未进行 Edge 验收。
- 对比截图在仓库同级 `review-0908/index.html` 第六项，示例子区「采购讨论」「质量讨论」保留在本地预览。仍是本地原型存储隔离，真实模型上下文隔离需后续 channel adapter 服务联调验证。
