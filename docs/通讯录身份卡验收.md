# 通讯录身份卡

身份卡沿用 Octo-Web ProfileDetailShell 的头像、信息区及操作区结构，通过 Semi Modal 和 Eva 公共身份组件适配。通讯录保持联系人／AI 分身两列、搜索、前两个分身与更多入口；人类和 AI 分别打开资料卡。

| 身份 | 信息与操作 |
| --- | --- |
| 人类 | 头像、姓名；其他人可发消息，自己不提供给自己发消息 |
| 云端分身 | AI 标、类型、简介（有数据才展示）、可点击的所属人；所属人可在同一弹窗查看并返回 |
| 本人的已有云端分身 | 进入现有我的 AI 团队会话 |
| 其他人的分身 | 展示群中 @ 协作说明，不因查看资料授予私人对话权限 |
| 数字员工 | 职责、公共／个人／项目归属；已添加到我的 AI 团队且当前账号可用时进入已有会话；不虚构个人主人 |
| Eva 项目管理专员 | 职责、当前可访问的服务项目、进入项目；无私聊操作 |

不增加部门、岗位、Octo 号、备注、好友关系、在线状态推测。公共头像比例和 AI 标保持既有合同。群成员与项目成员仍保留原有管理入口；查看资料不重建消息流或输入框。

## 数据与状态

- `009-3-contact-identities.js` 按稳定 ID 投影身份和允许的操作，不依据显示名称猜测身份。
- `009-3-identity-card.js` 提供各入口共用的卡片和头像适配。卡片负责所属人返回状态；路由负责跳转。
- 通讯录使用原生 React Route，原 DOM 注册桥已移除。
- 人类私聊的会话索引、消息和按账号隔离的草稿存放在成员业务 store，按双方账号 ID 复用会话。切换演示账号会重置内部会话组件；不合并上一个账号的临时私聊缓存。
- 弹窗使用标题栏下方的专用 Portal 宿主，宿主持续挂载，关闭时卸载 Modal。无需修改路由调度或操作 DOM 绕过 React。

## 本地验收（2026-09-07）

Microsoft Edge，1200 × 800，100% 缩放；通过 HTTP 访问 `npm start` 生成的页面。使用独立浏览器上下文，自动化脚本及截图保存在本地忽略目录 `artifacts/contact-identity/`。

- 搜索无结果、自己无发送按钮、分身所属人往返通过。
- 三次连续执行“更多分身 → 资料 → Esc 返回 → 关闭列表”，弹窗与遮罩均清理。
- 头像主图 56 px，主人头像 24.5 px，与公共 32:14 比例一致；mask/wrap 起点均为标题栏下方 37 px。
- 人类私聊发送、重复进入复用同一会话、聊天信息／资料往返及跨入口草稿保留通过。
- 切换到何静后，王宜林与林晓的私聊内容及输入区不残留。
- 数字员工资料不显示所属人，进入该员工已有会话；项目管理专员只进入可访问项目。
- 项目成员管理中添加单个分身后，仍可使用“管理分身 → 移除分身”。
- 通讯录、个人 Eva、我的 AI 团队入口往返通过，无页面 JavaScript 错误。个人 Eva 与我的 AI 团队的基准布局样式未修改，另存同视口截图核对。
- `npm test` 169 项通过（含身份解析、私聊隔离、头像合同及路由架构测试）；构建、manifest、项目、协作、消息路由、侧栏合同、补丁 hash、产物语法与 diff 检查通过。

用户已在本任务明确授权合入 main；Preview、Production 的部署 commit 与线上验收结果由 PR 和本次交付记录提供，不由本地检查推定。
